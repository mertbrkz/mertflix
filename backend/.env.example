# Postgres
DATABASE_URL=postgres://postgres:postgres@localhost:5432/mertflix

# Server
PORT=4000
CORS_ORIGIN=http://localhost:5173
JWT_SECRET=change_me
JWT_EXPIRES_IN=7d

# Email (optional). If not set, codes will be logged to console.
MAIL_FROM=no-reply@mertflix.local
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
