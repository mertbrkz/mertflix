{
  "name": "midterm-3355",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "lint": "npm --prefix frontend run lint",
    "test": "npm --prefix frontend run test",
    "server:dev": "npm --prefix backend run dev",
    "server:start": "npm --prefix backend run start",
    "server:migrate": "npm --prefix backend run migrate"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
